class LastWordLength {
    public static void main(String[] args) {
        String str = "Hi how are your friends";
        String[] ptr = str.split(" ");
        String length_last = ptr[ptr.length-1];
        System.out.println(length_last.length());
    }
}